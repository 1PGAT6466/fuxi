# 伏羲 v1.44 自我进化体系完整性检测报告

**检测时间**: 2026-07-10 12:24 (Asia/Shanghai)  
**检测人**: 帝八（系统架构专家）  
**工作目录**: `E:\easyclaw\伏羲-v1.44\repo`

---

## 总览

| # | 组件 | 状态 | 说明 |
|---|------|------|------|
| 1 | 自进化闭环 | ✅ 完成 | 反馈→学习→进化→生命周期全链路已实现 |
| 2 | 知识生命周期 | 🟡 骨架存在但未验证 | 事件记录+触发检查已实现，缺归档/删除链路 |
| 3 | Feature Flag | ✅ 完成 | 运行时开关+WebSocket广播已实现，无A/B测试 |
| 4 | 评测体系 | 🟡 骨架存在但未验证 | 管线已就绪，缺ground_truth数据集+LLM-as-Judge |
| 5 | Wiki引擎 | ✅ 完成 | CRUD+向量检索+版本管理+交叉引用全链路 |
| 6 | 知识图谱 | 🟡 骨架存在但未验证 | 路由+多跳已实现，有 NameError bug |
| 7 | 数据流 | 🟡 骨架存在但未验证 | ChromaDB/SQLite已就绪，Redis未连接 |

---

## 1. 自进化闭环

**文件**: `src/evolution/feedback_loop.py` + `src/evolution/evolution_gua.py`

### 状态: ✅ 完成

### 检测详情

**反馈收集→分析→优化→验证链路**:

| 环节 | 实现位置 | 状态 |
|------|---------|------|
| 反馈收集 | `services/feedback_store.py` → `log_feedback_unified()` | ✅ |
| 去重 | MD5签名 + TTLCache (300s窗口, 2000条上限) | ✅ |
| 批量学习触发 | buffer ≥ 20条时自动调用 `learner.learn_from_feedback()` | ✅ |
| 术语权重调整 | `services/learner.py` → `update_term_weight()` | ✅ |
| 知识进化 | `services/evolver.py` → `evolve_graph()` | ✅ |
| 生命周期检查 | `services/knowledge_lifecycle.py` → `check_triggers()` | ✅ |
| 验证/健康检查 | `evolution_gua.py` → `health_summary()` + 降级矩阵 | ✅ |

**日/周/月循环**:
- 日循环: `dream_cycle.py` → `DreamCycle.run()` (digest → enrich → consolidate → gap_scan → report)
- 已有实际运行记录: `data/dream_reports/dream_report_20260708_0358.md`
- 周/月循环: **未显式实现**，但日循环可聚合

**EvolutionGua 统一入口**:
- 继承 `GuaBase`，支持 `execute({"action": "feedback/learn/evolve/lifecycle"})`
- 降级矩阵: 4条规则 (全部不可用→空操作, 学习者不可用→跳过, 进化器不可用→跳过)
- 延迟初始化四大组件: FeedbackLoop, EvolutionLearner, EvolutionEvolver, EvolutionLifecycle

**不足**:
- 周/月级别的聚合分析未实现（可通过cron聚合日报告弥补）
- 验证环节仅限健康检查，无自动化A/B评测

---

## 2. 知识生命周期

**文件**: `src/services/knowledge_lifecycle.py` + `src/evolution/lifecycle.py`

### 状态: 🟡 骨架存在但未验证

### 检测详情

**知识创建→更新→归档→删除链路**:

| 环节 | 状态 | 说明 |
|------|------|------|
| 创建 | ✅ | `record_event()` 写入JSONL日志 |
| 更新 | 🟡 | 通过触发条件检测(entity_not_found等)，但无自动更新机制 |
| 归档 | 🔴 | 未实现 |
| 删除 | 🔴 | 未实现 |

**触发条件** (5种，均已实现):
- `entity_not_found`: 阈值10/7天
- `synonym_variant`: 阈值5/7天
- `missing_relation`: 阈值3/7天
- `empty_result`: 阈值20/7天
- `user_doubt`: 阈值5/7天

**置信度分级**:
- high (≥0.9) → auto_add
- medium (≥0.7) → pending_queue
- low (<0.7) → ignore

**知识版本管理**: 未实现。事件以JSONL追加方式存储，无版本号、无快照、无回滚。

**数据目录**: `data/knowledge_lifecycle/` 存在但为空（无事件记录）

**不足**:
- 归档/删除链路缺失
- 无知识版本管理
- `classify_confidence()` 的阈值动作(auto_add/pending_queue)未实现下游处理
- `knowledge_lifecycle` feature flag 默认关闭

---

## 3. Feature Flag

**文件**: `src/services/feature_flags.py`

### 状态: ✅ 完成

### 检测详情

**运行时开关控制**:
- `is_enabled(feature)` → 读取flag，10秒缓存自动刷新
- `set_flag(feature, enabled)` → 写入JSON文件 + 广播WebSocket变更
- `get_all_flags()` → 返回所有flags + 元数据
- 持久化: `data/feature_flags.json`

**已定义的Flags** (22个):
- 核心Flag: `shaoyang_sag_extract`, `taiyang_multi_hop`, `taiyang_seed_score`
- Phase B: `taiyang_sag_pipeline`, `taiyang_path_a`, `taiyang_event_search`, `taiyang_sql_multi_hop`
- 增强Flag: `enhanced_pipeline` (默认关闭)
- 基础Flag: `graphrag_multi_hop`, `query_planner`, `table_structured_search`, `multimodal_rag`, `knowledge_lifecycle`, `siliconflow_rerank`, `self_rag_check`, `crag_rewrite`, `query_rewrite`, `hyde`, `wiki_search`, `table_view`, `session_memory`
- Dream Cycle: `enable_dream_cycle_notifications`, `enable_gap_llm`

**WebSocket广播**: `_broadcast_change()` → `feature_flags_ws.broadcast_flag_change()`

**A/B测试**: 未实现。仅有布尔开关，无流量分配、无实验组/对照组、无统计显著性检验。

**不足**:
- 无A/B测试框架
- flag冲突检测缺失（如 `enhanced_pipeline` 和子flag的关系未校验）

---

## 4. 评测体系

**文件**: `src/api/evaluation.py` + `src/services/eval_pipeline.py` + `src/services/eval_automation.py` + `src/services/eval_dataset.py`

### 状态: 🟡 骨架存在但未验证

### 检测详情

**API端点**:
| 端点 | 方法 | 状态 |
|------|------|------|
| `/api/evaluation/overview` | GET | ✅ |
| `/api/evaluation/datasets` | GET | ✅ |
| `/api/evaluation/tasks` | GET | ✅ |
| `/api/evaluation/results` | GET | ✅ |
| `/api/evaluation` | POST | ✅ (异步任务队列) |

**评测指标**:
| 指标 | 实现位置 | 状态 |
|------|---------|------|
| Recall@1/3/5/10 | `eval_pipeline.py` → `_calculate_recall_at_k()` | ✅ |
| MRR | `eval_pipeline.py` → `_calculate_mrr()` | ✅ |
| LLM-as-Judge | `eval_pipeline.py` 文件头提及 | 🔴 未实现 |
| 检索延迟 | `eval_automation.py` → `_eval_performance()` | ✅ |
| 退化检测 | `eval_automation.py` → `_check_degradation()` | ✅ |

**评测数据集**:
- 种子用例: `eval_dataset.py` 定义了30条 (fact/comparison/procedural/causal/aggregation)
- 数据库: `~/kb-server/data/eval.db` (远程服务器路径)
- `ground_truth.json`: **不存在**
- 已有报告: `data/evaluation/reports/smoke_test_baseline.json`

**烟雾测试**: `run_smoke_test()` 验证搜索API格式 + 嵌入API健康

**不足**:
- `ground_truth.json` 缺失，无法执行完整的 Recall@10/MRR 评测
- LLM-as-Judge 未实现
- 评测数据集路径指向远程服务器 (`~/kb-server/data/eval.db`)，本机不可直接访问
- 种子用例无 `expected_answer`/`expected_chunks` 字段，无法计算检索指标

---

## 5. Wiki引擎

**文件**: `src/services/wiki.py` (兼容层) → 实际实现 `src/taiyang/wiki.py`

### 状态: ✅ 完成

### 检测详情

**Wiki页面创建→索引→检索链路**:

| 环节 | 实现 | 状态 |
|------|------|------|
| 创建 | `create_page()` → SQLite + UUID v4 | ✅ |
| 更新 | `update_page()` → 乐观锁(版本号检查) | ✅ |
| 删除 | `delete_page()` → SQLite DELETE | ✅ |
| 摘要索引 | `sync_vectors()` → ChromaDB `wiki_summaries` collection | ✅ |
| 向量检索 | `vector_search_wiki()` → cosine similarity | ✅ |
| 全文检索 | `search_content()` → SQLite LIKE | ✅ |
| 标题检索 | `search_by_title()` → SQLite LIKE | ✅ |
| 标签检索 | `search_by_tag()` → SQLite LIKE | ✅ |
| 交叉引用 | `add_cross_link()` + `get_linked_pages()` | ✅ |
| 版本历史 | `update_page()` 写入 `wiki_history` 表 | ⚠️ 表未创建 |

**Wiki数据存储**:
- SQLite: `data/worldtree.db` (wiki_pages + wiki_cross_links 表)
- ChromaDB: `wiki_summaries` collection (摘要向量)

**已知问题**:
1. `wiki_history` 表在 `update_page()` 中被引用（INSERT），但 `_init_db()` 中未创建该表，会导致首次版本记录写入时抛 `sqlite3.OperationalError`
2. `feature_flags.json` 中 `wiki_search` 默认关闭

**功能完整度**: CRUD + 多种检索 + 向量索引 + 版本管理 + 模板聚合生成，功能完备。

---

## 6. 知识图谱

**文件**: `src/services/graph_router.py` (兼容层) → 实际实现 `src/taiyang/graph_router.py`

### 状态: 🟡 骨架存在但未验证

### 检测详情

**图谱构建→查询→更新链路**:

| 环节 | 实现 | 状态 |
|------|------|------|
| 图谱加载 | `load_graph()` → `knowledge_graph.json` | ✅ |
| 实体匹配 | `_match_entities_in_query()` → 子串匹配 | ✅ |
| 模糊匹配 | `fuzzy_match_entity()` → 子串+词重叠 | ✅ |
| 分类路由 | `route_to_categories()` → 17类映射 | ✅ |
| 意图检测 | `detect_query_intent()` → 关键词匹配 | ✅ |
| 邻域扩展 | `route_entity_with_neighbors()` → 1-hop遍历 | ✅ |
| Wiki链接 | 自动匹配实体到Wiki页面 | ⚠️ 有bug |
| 同义词扩展 | `expand_query_with_synonyms()` | ✅ |
| 多跳搜索 | `multi_hop_search()` → `graph_traversal` | ✅ |
| 图谱更新 | `services/evolver.py` → `evolve_graph()` | ✅ |

**知识图谱数据**: `data/knowledge_graph.json`
- 节点: 7个 (伏羲, ChromaDB, 震卦, 坤卦, 系统, 模块, 框架)
- 边: 7条 (uses, contains, related_to)
- 编码问题: 中文字符显示为乱码（UTF-8编码问题，不影响功能）

**已知Bug** (`route_entity_with_neighbors()`):
```python
# 第一个 Phase 2 块 (~行293): wiki_links 未定义就使用
wiki_links.append({...})  # NameError! wiki_links 在此时尚未初始化

# 第二个 Phase 2 块 (~行327): 正确初始化
wiki_links = []
```
**影响**: 当匹配到实体且Wiki页面存在时，会抛出 `NameError`，导致Wiki自动链接功能失效。该bug被外层 `try/except` 捕获，不会崩溃但功能静默失败。

**不足**:
- 知识图谱数据量极少（7节点7边），尚未大规模填充
- `ENTITY_TYPE_TO_CATEGORY` 有重复key (`"operation_manual"` 出现两次)
- `INTENT_KEYWORDS` 有重复key (`"公司制度"` 出现两次)
- Wiki链接的 Phase 2 代码块重复（疑似合并冲突遗留）

---

## 7. 数据流

### ChromaDB

**状态**: ✅ 已连接

- 配置: `data/chromadb/` (PersistentClient)
- Collection: `kb_chunks` (主向量库) + `wiki_summaries` (Wiki摘要)
- 文件: `data/chromadb/chroma.sqlite3` + segment目录
- 封装: `src/db/vector_store.py` (增删查 + 故障自愈 + 批量嵌入代理)
- 权限检查: `_check_chromadb_permissions()` 已实现

### Redis

**状态**: 🔴 未连接

- 配置: `.env` 中已配置 `REDIS_HOST=localhost:6379`
- 实现: `src/infra/task_queue.py` → `RedisStreamTaskQueue` 类已完整实现
- **问题**: `get_task_queue()` 在未初始化时抛 `RuntimeError("任务队列未初始化")`
- Redis 作为任务队列（Stream模式）和缓存层，但当前进程未建立连接
- 影响: 异步评测任务 (`POST /api/evaluation`) 会失败

### SQLite

**状态**: ✅ 已连接

| 数据库 | 路径 | 用途 |
|--------|------|------|
| chunks.db | `data/chunks.db` | 文档分块存储 (MemoryStore) |
| worldtree.db | `data/worldtree.db` | Wiki页面 + 交叉引用 |
| memory.db | `data/memory.db` | 会话记忆 |
| chat_sessions.db | `data/chat_sessions.db` | 聊天会话 |
| login_rate.db | `data/login_rate.db` | 登录限流 |
| audit.db | `data/audit.db` | 审计日志 |
| eval.db | `~/kb-server/data/eval.db` | 评测数据集（远程） |

所有本地SQLite数据库均存在且可访问。MemoryStore实现了LRU缓存 + 自动修复（从.bak恢复）。

---

## 关键发现汇总

### 🔴 需立即修复

1. **graph_router.py NameError Bug**: `route_entity_with_neighbors()` 中 `wiki_links` 在第一个Phase 2块中未初始化就使用，导致Wiki自动链接静默失败

2. **Redis未连接**: 任务队列无法使用，影响异步评测等功能

### 🟡 需要完善

3. **知识生命周期**: 缺少归档/删除链路和版本管理
4. **评测数据集**: `ground_truth.json` 缺失，无法执行完整评测
5. **LLM-as-Judge**: 评测管线提及但未实现
6. **wiki_history表**: `update_page()` 引用但 `_init_db()` 未创建
7. **知识图谱数据**: 仅7节点，需大规模填充
8. **图谱JSON编码**: 中文字符乱码（UTF-8问题）

### ✅ 已完成且可用

9. 反馈闭环（去重+批量学习+进化）完整可用
10. Dream Cycle夜间消化循环已运行
11. Feature Flag运行时开关 + WebSocket广播
12. Wiki引擎CRUD + 向量检索 + 版本管理
13. ChromaDB/SQLite数据层稳定

---

## 修复建议

### P0 (立即)

1. **修复 graph_router.py NameError**:
   ```python
   # 在 route_entity_with_neighbors() 函数开头添加:
   wiki_links = []
   ```

2. **Redis连接**: 确认Redis服务是否已安装并启动，或改用内存队列作为fallback

### P1 (本周)

3. **创建 wiki_history 表**: 在 `WikiEngine._init_db()` 中添加:
   ```sql
   CREATE TABLE IF NOT EXISTS wiki_history (
       page_id TEXT, version INTEGER, content_snapshot TEXT,
       summary_snapshot TEXT, changed_at TEXT, source TEXT,
       PRIMARY KEY (page_id, version)
   )
   ```

4. **构建 ground_truth.json**: 为30条种子用例补充 `expected_answer` 和 `expected_chunks` 字段

5. **修复编码问题**: 检查 `knowledge_graph.json` 写入时的编码参数

### P2 (本月)

6. 实现知识生命周期的归档/删除链路
7. 实现LLM-as-Judge评测
8. 大规模填充知识图谱
9. 实现A/B测试框架

---

*报告生成: 帝八 · 伏羲 v1.44 架构检测 · 2026-07-10*
